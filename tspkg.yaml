name: pcm
version: 202002

build: |
  make clean

  make -j8
  install *.x bin/

  install *.h include/pcm/
  install exceptions/*.hpp include/pcm/exceptions/

  install *.a lib/

  cd pcm.so
  make -j8
  cd ../

  install *.so lib/
